<Page x:Class="OpenshiftOPSCenter.App.Views.UserManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OpenshiftOPSCenter.App.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Benutzerverwaltung">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Benutzerinformationen" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <!-- Benutzerinformationen -->
            <GroupBox Header="Benutzerdetails" Margin="0,0,0,10">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Benutzername:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,5"/>
                    <TextBlock x:Name="UsernameText" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

                    <TextBlock Text="Vollständiger Name:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,5"/>
                    <TextBlock x:Name="FullNameText" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

                    <TextBlock Text="Letztes Login:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,5"/>
                    <TextBlock x:Name="LastLoginText" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>

                    <TextBlock Text="Rolle:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,5"/>
                    <TextBlock x:Name="RoleText" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"/>
                </Grid>
            </GroupBox>

            <!-- LDAP-Gruppen -->
            <GroupBox Header="LDAP-Gruppen" Margin="0,0,0,10">
                <ListView x:Name="LdapGroupsList" Margin="10" MaxHeight="150">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUserGroup}" Value="True">
                                                <Setter Property="Foreground" Value="Green"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>

            <!-- Benutzerrechte -->
            <GroupBox Header="Benutzerrechte" Margin="0,0,0,10">
                <ListView x:Name="RightsList" Margin="10" MaxHeight="150">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Recht" Width="300"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>

            <!-- Button-Berechtigungen verwalten -->
            <GroupBox Header="Button-Berechtigungen verwalten" Margin="0,0,0,10">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Rolle: " FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox x:Name="RoleComboBox" Width="150" SelectionChanged="RoleComboBox_SelectionChanged" Margin="0,0,10,0">
                            <ComboBoxItem Content="PowerUser"/>
                            <ComboBoxItem Content="User"/>
                            <ComboBoxItem Content="OPCUA"/>
                        </ComboBox>
                        <TextBlock Text="(Admin hat automatisch alle Rechte)" VerticalAlignment="Center" FontStyle="Italic"/>
                    </StackPanel>
                    
                    <ListView x:Name="ButtonRightsList" Grid.Row="1" SelectionMode="Multiple">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Recht" DisplayMemberBinding="{Binding Description}" Width="300"/>
                                <GridViewColumn Header="Button" DisplayMemberBinding="{Binding ButtonName}" Width="200"/>
                                <GridViewColumn Header="Zugewiesen" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsAssigned}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="SaveRightsButton" Content="Rechte speichern" Width="120" Click="SaveRightsButton_Click" Margin="0,0,10,0"/>
                        <Button x:Name="ResetDefaultsButton" Content="Standardrechte wiederherstellen" Width="200" Click="ResetDefaultsButton_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </StackPanel>
    </Grid>
</Page> 