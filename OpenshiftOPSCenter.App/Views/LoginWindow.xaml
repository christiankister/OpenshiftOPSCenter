<Window x:Class="OpenshiftOPSCenter.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenshiftOPSCenter.App.Views"
        mc:Ignorable="d"
        Title="Anmeldung" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Anmeldung" 
                 FontSize="24" 
                 Margin="0,0,0,20" 
                 Grid.Row="0"/>

        <TextBlock x:Name="CurrentUserText"
                 Grid.Row="1"
                 Margin="0,0,0,5"/>

        <TextBlock x:Name="FullNameText"
                 Grid.Row="2"
                 Margin="0,0,0,20"/>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Benutzername:"/>
            <TextBox x:Name="UsernameTextBox" 
                    Margin="0,5,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="Passwort:"/>
            <PasswordBox x:Name="PasswordBox" 
                        Margin="0,5,0,20"/>
        </StackPanel>

        <TextBlock x:Name="ErrorMessage"
                 Grid.Row="5"
                 Foreground="Red"
                 TextWrapping="Wrap"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom">
            <Button Content="Abbrechen" 
                    Width="100" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="Anmelden" 
                    Width="100" 
                    Height="30"
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 